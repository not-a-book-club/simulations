[package]
name = "simulations"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.9", default-features = false }

[dev-dependencies]
image = "0.25"
indoc = "2"
pretty_assertions = "1.4"
rstest = "0.24"

[features]
default = ["std"]
std = []
